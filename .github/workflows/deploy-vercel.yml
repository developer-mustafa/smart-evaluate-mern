name: Vercel Deployment

on:
  push:
    branches:
      - main

env:
  VERCEL_ORG_ID: team_3cgAcbgEyvJTMswXN2ixRkXI
  VERCEL_PROJECT_ID: prj_uANNjg2ywv9BadR8N5mxa7pfiEmb
  VERCEL_TOKEN: eRomY1lSL1nk03l9m6okonx7

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Vercel CLI
        run: npm install --global vercel@latest
        
      - name: Deploy to Vercel
        run: cd client && vercel --prod --token=${{ env.VERCEL_TOKEN }} --yes
